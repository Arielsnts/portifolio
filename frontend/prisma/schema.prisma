generator client {
  provider = "prisma-client"
  output   = "../src/app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Projeto {
  id     Int      @id @default(autoincrement())
  nome   String
  img    String
  tags   String[]
  link   String
  ordem  Int      @default(0)
}